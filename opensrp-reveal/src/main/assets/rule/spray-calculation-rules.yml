---
name: step1_business_status
description: Task business status
priority: 1
condition: "true"
actions:
- "calculation = (step1_structureType !='' && step1_structureType != 'Residential Structure') ? 'Not Sprayable' : step1_sprayStatus "