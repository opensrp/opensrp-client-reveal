---
name: step1_business_status
description: Task business status
priority: 1
condition: "true"
actions:
  - "calculation = (step1_structureType != '' && step1_structureType == 'Non-Residential Structure') ? 'Not Sprayable' :  (helper.ifNull(step1_numbersprayed_ddt,0) + helper.ifNull(step1_numbersprayed_delta,0) + helper.ifNull(step1_numbersprayed_actellic,0)  > 0 ) ? ((helper.ifNull(step1_numbersprayed_ddt,0) + helper.ifNull(step1_numbersprayed_delta,0) + helper.ifNull(step1_numbersprayed_actellic,0)  < step1_sprayedRooms) ? 'Partially Sprayed' : 'Sprayed') : 'Not Sprayed' "