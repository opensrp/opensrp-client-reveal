---
name: step1_business_status
description: Task business status
priority: 1
condition: "true"
actions:
- "calculation = step1_trap_type.contains('CDC') || step1_trap_type.contains('HLC') || step1_trap_type.contains('Trap/net') || step1_trap_type.contains('Backpack') || step1_trap_type.contains('Swingnet') ? 'Complete' : 'Incomplete' "